import * as React from "react";
import { SvgProps, NumberProp } from "react-native-svg";
interface Props extends SvgProps {
    size?: NumberProp;
}
declare const Square2StackIcon: ({ size, ...props }: Props) => React.JSX.Element;
export default Square2StackIcon;
