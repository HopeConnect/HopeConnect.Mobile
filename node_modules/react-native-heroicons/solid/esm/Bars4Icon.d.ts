import * as React from "react";
import { SvgProps, NumberProp } from "react-native-svg";
interface Props extends SvgProps {
    size?: NumberProp;
}
declare const Bars4Icon: ({ size, ...props }: Props) => React.JSX.Element;
export default Bars4Icon;
