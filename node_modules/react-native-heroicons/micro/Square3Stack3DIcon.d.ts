import * as React from "react";
import { SvgProps, NumberProp } from "react-native-svg";
interface Props extends SvgProps {
    size?: NumberProp;
}
declare const Square3Stack3DIcon: ({ size, ...props }: Props) => React.JSX.Element;
export default Square3Stack3DIcon;
